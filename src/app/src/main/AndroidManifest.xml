<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hcmute.edu.vn.nhom02.foodyproject">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Foody"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".viewmodel.RestaurantImageActivity"/>
        <activity android:name=".viewmodel.RestaurantTabhost" android:theme="@style/AppThemeNoActionbar" />
        <activity android:name=".viewmodel.MenuActivity" />
        <activity android:name=".viewmodel.ProvinceList" />
        <activity android:name=".viewmodel.RestaurantResult" />
        <activity android:name=".viewmodel.RestaurantList" />
        <activity android:name=".viewmodel.RestaurantDetail"/> <!-- <activity android:name=".RestaurantDetail" /> -->
        <activity android:name=".viewmodel.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>